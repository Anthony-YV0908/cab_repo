var maxIndex = (paginationBar5.totalCount5 / paginationBar5.pagination5.take);
maxIndex = Math.ceil(maxIndex);

paginationBar5.pagination5.index = maxIndex;
paginationBar5.run5(true); 
paginationBar5.handlePagination5();