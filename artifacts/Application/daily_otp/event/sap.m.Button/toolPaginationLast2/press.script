var maxIndex = (paginationBar2.totalCount2 / paginationBar2.pagination2.take);
maxIndex = Math.ceil(maxIndex);

paginationBar2.pagination2.index = maxIndex;
paginationBar2.handleModePagination3(); // Fetch data for current mode
paginationBar2.updatePaginationUI3(); // Reflect UI changes
