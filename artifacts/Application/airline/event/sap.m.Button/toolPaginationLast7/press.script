var maxIndex = (paginationBar4.totalCount4 / paginationBar4.pagination4.take);
maxIndex = Math.ceil(maxIndex);

paginationBar4.pagination4.index = maxIndex ;
paginationBar4.handleModePagination4(); // Fetch data for current mode
paginationBar4.updatePaginationUI4(); // Reflect UI changes
