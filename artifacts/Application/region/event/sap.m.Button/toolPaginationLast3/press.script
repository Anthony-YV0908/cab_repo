var maxIndex = (paginationBar6.totalCount6 / paginationBar6.pagination6.take);
maxIndex = Math.ceil(maxIndex);

paginationBar6.pagination6.index = maxIndex ;
paginationBar6.handleModePagination6(); // Fetch data for current mode
paginationBar6.updatePaginationUI6(); // Reflect UI changes
